components:
  schemas:
    AddressResponse:
      required:
        - 'id'
        - 'address1'
        - 'city'
        - 'state'
        - 'country'
        - 'postalCode'
        - 'createdAt'
        - 'updatedAt'
      properties:
        id:
          type: 'number'
        address1:
          type: 'string'
        address2:
          type: 'string'
        address3:
          type: 'string'
        city:
          type: 'string'
        state:
          type: 'string'
        country:
          type: 'string'
        postalCode:
          type: 'string'
        createdAt:
          type: 'string'
        updatedAt:
          type: 'string'

    CreateAddressBody:
      required:
        - 'address1'
        - 'city'
        - 'state'
        - 'country'
        - 'postalCode'
      properties:
        address1:
          type: 'string'
        address2:
          type: 'string'
        address3:
          type: 'string'
        city:
          type: 'string'
        state:
          type: 'string'
        country:
          type: 'string'
        postalCode:
          type: 'string'
